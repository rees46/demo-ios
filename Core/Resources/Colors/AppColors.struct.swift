import SwiftUI

struct AppColors {
    static let selectedForeground = Color.white
    static let unselectedForeground = Color.gray
    static let ratingColor = Color.yellow
    static let foregroundColor = Color.white
    static let colorBlack = Color.black
}
